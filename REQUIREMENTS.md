# 当前需求与验收约束

本文件依据 PROJECT.md、当前实现及 2026-09-05 用户要求补建；历史比赛参数以现有代码为准。

## 模块与运动控制

- main.c 保持初始化入口；BSP、Device、Algorithm、Robot、Task 使用现有分层。
- JY60 使用 USART2，保留 Jy61P_* 兼容接口，当前用户将串口波特率由 9600 改为 115200。底盘电机使用 USART3；仓库转盘使用 USART6，地址 0x05。
- 保持现有电机安装方向、RPM/毫米/角度单位和比赛动作参数；本次不调整 PATH、RZ、STAIR、CANGKU 运动表。
- 动作由现有底盘任务串行执行；保留 STOP、IMU 掉线及通信失败处理。

## 指令接收与电机映射（2026-09-05 更新）

- 电机地址按用户最新说明：1 右前、2 左前、3 左后、4 右后；逻辑麦轮矩阵仍使用左前、右前、左后、右后顺序。
- 删除 Ready/Busy、舵机、仓库和转盘状态在串口接收层的拦截；合法指令进入容量 4 的 FIFO，单任务串行执行，队列满才返回 ERR QUEUE_FULL。
- 舵机启动失败及动作错误不永久关闭底盘任务。启动运动准备失败允许后续命令重试。
- STOP 清空待执行队列并取消当前动作；新指令入队不得清除当前 STOP，出队与 STOP 并发时不能执行失效指令。
- 保留参数、协议帧、STOP、IMU 掉线、通信失败、动作完成与超时判断，这些用于决定动作是否实际完成，不作为跨设备的入队锁。
- 保留用户本轮开始前将 Core/Src/usart.c 的 USART2 改为 115200 的改动；此前资料和 CubeMX 配置为 9600，需以实际 IMU 配置确认一致，当前不擅自覆盖。

## 底盘距离精度要求

- 平移加速度按用户要求提高至原来的两倍：300→600 RPM/s，巡航仍为普通 450 RPM、斜行 255 RPM；减速度沿用 120/40 RPM/s。

- 用户要求当前普通平移巡航提高到 3 倍：150→450 RPM，斜行 85→255 RPM；单轮上限 460 RPM。保持 0.1 RPM 编码及 S_Vel_IS=Enable，不以单位不匹配提速。保留减速包络，短距离允许达不到峰值。验证长距离能达到 450 RPM、限幅及停车误差，高速实车停车精度待测。

- 用户实测 ROT CCW 10 向右、右转 YAW 为负：统一修正车体旋转到轮速的方向映射，使正角速度对应左转；不反转 IMU 读数或仅单独反转 PD。主机测试覆盖左右定角旋转收敛、航向扰动减小及平移距离回归，实车复测 CCW/CW 各 10 度后再验证 F 100。

- 用户更换底盘驱动固件为 X42S，反馈停车时除左后轮外其余轮子回滚，要求四轮一起硬停。
- 保留行驶减速包络，但定距结束立即用一帧广播硬停，删除停车后 300 ms 航向修正和软停等待。行驶航向 PD 沿用原基准，修正幅度随平移速度减小。
- 零车体速度请求、STOP、视觉提前截断和故障停车统一使用立即停止；不失能电机，不在停车命令后发送可能释放旧缓存的 FF。非零末速度保持连续衔接。
- 测试必须验证单帧覆盖四轮、不同轮速归零、部分速度发送失败后不触发旧缓存、停车通信失败重试并报告错误。串口发送成功不等于四台驱动器执行确认，实车同步停车仍需验收。

- 用户反馈提速后前进、横移距离不一致，且与指定距离有误差；尚无实测数据。
- 保留 F6 同步速度模式、现有极坐标 API、方向矩阵、航向参数和比赛动作表。
- 平移加速以 RPM/s 限制；减速由剩余积分距离确定，轮速限幅不能导致固定时间提前结束。
- 按成功同步发送时刻、量化后轮速反解积分；该距离只是指令估计，不是编码器或地面位置反馈。
- 前进/后退和左移/右移分两轴独立标定；用户当前已恢复两轴 1.0 系数，保留其设置，不自行填写猜测补偿值。
- 零末速度：理想无打滑主机模型距离误差不超过 1 mm；验证短距离、多方向、多速度、发送耗时变化和 tick 回绕。
- 保留非零末速度连续衔接；该模式仍存在控制周期及通信延迟带来的交接距离误差，不适用零末速度的 1 mm 模型标准。
- 保留 STOP、视觉提前停车、IMU 掉线和 UART 失败的停车路径；拒绝非有限巡航速度及低于 0.1 RPM 的不可可靠编码速度。
- 实车分别测量前进和横移，在同一地面与负载下至少重复三次；不能用 STATUS 的 DIST 代替尺量距离。

## RFID 自动读卡要求

- 依据用户提供 V23C 压缩包内《高频读写器使用手册 V1.0.5》，使用 UART8 PE0=RX、PE1=TX，默认 9600-8-N-1、地址 0x20。
- 使用自动读卡号、主动上传模式。STM32 不在上电时写入读写器 EEPROM 参数，也不自动写标签。
- 不固定 ID，不做 1..9 映射。保存 32 位完整 UID，按线上 4 字节顺序显示 8 位十六进制，包括前导零；卡类型的 2 字节不属于 UID。
- 仅完整长度、地址、命令、成功状态、异或取反校验正确的卡号帧产生 UID。支持自动卡号帧和自动卡号+数据块帧中的 UID；读块数据本身不解释成编号。
- 接收半帧超时、错误长度、校验错误和 UART 错误后可恢复接收。查询状态不得消费 BALL 等待的数据。
- UART5 `RFID` 只输出接收及完整卡号状态，不执行机械动作。

## BALL 记录与异常

- 夹取动作组 2 开始前清除旧接收数据，保留夹取期间收到的卡号，组 2 完成后最多等待 10 秒。
- 同一批最多记录 5 个不重复的完整 UID。重试保留成功记录，满 5 个后再次运行不继续抓取。`BallSequence_Init()`/重启开始新的缓存批次。
- 仅仓库转盘步骤报告成功后追加 UID；仓库机械计数仍为 6 格，不由卡号容量重新定义。
- RFID 超时、STOP 或转盘失败后仍执行组 1 回位，再报告结果；舵机自身失败保留错误锁。
- 转盘完成仍沿用现有时间估计，不能作为硬件反馈已确认的依据。
- CANGKU 尚未按 UID 分拣。本次仅提供真实卡号记录接口，不假设卡号的业务含义。

## 验收

- STATUS 的角度、航向误差、修正与距离字段必须显示数值；兼容未启用浮点 printf 的 GCC newlib-nano，不能以空字段代替零值。角度保留两位小数和负号，距离取整。

- 执行真实 RFID/BALL C 代码的主机测试：手册样例、完整 UID、错误校验、半帧超时、重复卡、重试、容量、STOP、转盘失败和读卡超时。
- 每次代码修改后执行 .vscode/build.ps1，并报告实际结果；Keil 不可用时同时报告 GCC 构建结果。
- 实物验收：先独立刷卡并查询 RFID，再验证 BALL 抓取时机和卡号顺序。未做实物验收不能宣称读卡器已可用。
